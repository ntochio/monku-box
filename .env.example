# Copy to .env.local for local overrides (never commit real secrets)
# NEXT_PUBLIC_* はブラウザに露出します

# 例: 将来の外部 STT / LLM（未使用でもキー名のみ定義可）
# STT_API_KEY=
# LLM_API_KEY=

# PoC データファイルパス（任意）
# MONKU_DATA_DIR=
